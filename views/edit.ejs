<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white font-sans transition-all duration-300">
  <div class="min-h-screen w-full px-6 py-10 sm:px-10">
    
    <!-- 🔙 Back Button -->
    <a href="/" class="inline-block mb-10 text-blue-400 hover:text-blue-300 transition duration-200 font-medium">
      ⬅️ Go to Home
    </a>

    <!-- 🧾 Heading -->
    <h1 class="text-3xl font-semibold text-white mb-6 tracking-tight">
      ✏️ Rename Your Task
    </h1>

    <!-- ✍️ Edit Form -->
    <form action="/edit" method="post" class="space-y-5 max-w-xl">
      
      <!-- Previous Name -->
      <input
        class="block w-full px-5 py-3 bg-zinc-800 text-white rounded-md outline-none placeholder:text-zinc-400 focus:ring-2 focus:ring-blue-500 transition duration-200"
        type="text"
        name="previous"
        placeholder="📝 Previous Filename"
        value="<%= filename %>"
        required
      />

      <!-- New Name -->
      <textarea
        class="block w-full px-5 py-3 bg-zinc-800 text-white rounded-md resize-none outline-none placeholder:text-zinc-400 focus:ring-2 focus:ring-blue-500 transition duration-200"
        rows="3"
        name="new"
        placeholder="✏️ New Filename"
        required
      ></textarea>

      <!-- Submit Button -->
      <input
        class="bg-yellow-500 hover:bg-yellow-600 px-6 py-2 rounded-md font-semibold text-zinc-900 cursor-pointer transition duration-300"
        type="submit"
        value="Update Name"
      />
    </form>
  </div>
</body>
</html>
