<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white font-sans transition-colors duration-300">
    <div class="main w-full min-h-screen p-10">
        <!-- 🌟 Heading -->
        <h1 class="text-5xl font-bold tracking-tight text-center mb-12 text-white drop-shadow-md">
            🗂️ TASK <span class="text-blue-400">MANAGER</span>
        </h1>
        <!-- Form -->
        <div
            class="form max-w-2xl mx-auto bg-zinc-800 rounded-xl p-6 shadow-lg hover:shadow-blue-600 transition-shadow duration-300">
            <form action="/create" method="post" class="space-y-4">
                <input
                    class="block w-full px-5 py-3 bg-zinc-700 rounded-md outline-none placeholder:text-zinc-400 focus:ring-2 focus:ring-blue-500 transition duration-200"
                    type="text" name="title" placeholder="📝 Title goes here" required />
                <textarea
                    class="block w-full px-5 py-3 bg-zinc-700 rounded-md resize-none outline-none placeholder:text-zinc-400 focus:ring-2 focus:ring-blue-500 transition duration-200"
                    rows="4" name="details" placeholder="🧾 Write your task details." required></textarea>
                <div class="text-right">
                    <input
                        class="bg-blue-600 hover:bg-blue-700 px-6 py-2 rounded-md font-medium cursor-pointer transition duration-300"
                        type="submit" value="Create Task" />
                </div>
            </form>
        </div>

        <!-- Tasks Section -->
        <div class="tasks mt-12 flex flex-wrap gap-6 justify-center">
            <% if (files.length> 0) { %>
                <% files.forEach(function(val) { %>
                    <div
                        class="task bg-zinc-800 hover:bg-zinc-700 transition-all duration-300 px-5 py-6 rounded-xl shadow-md hover:shadow-blue-500 min-w-[250px] max-w-xs w-full">
                        <h1 class="text-white text-2xl font-semibold tracking-tight truncate">
                            <%= val %>
                        </h1>
                        <a class="text-blue-400 mt-4 inline-block hover:text-blue-300 transition duration-200"
                            href="/file/<%= val %>">
                            🔍 Read more
                        </a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h1 class="text-zinc-500 text-center text-xl mt-10">No tasks yet.</h1>
                            <% } %>
        </div>
    </div>
</body>

</html>